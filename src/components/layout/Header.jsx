import React from "react";

const Header = () => {
  const appName = "Marla Calculation App";
  return (
    <header className="app-header">
      <h1>{appName}</h1>
    </header>
  );
};

export default Header;
